
version: 2

models:
    - name: customers
      description: "A Sample Customer model"
      columns:
          - name: customer_id
            description: "The primary key for this table"
            tests:
                - unique

